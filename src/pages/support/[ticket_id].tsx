// import { useRouter } from 'next/router';
// import React from 'react';

// import { TicketEmptyState } from '@/devlink/TicketEmptyState';
// import SupportTicketDetails from '@/src/components/Support/SupportTicket';

// function SupportTicket() {
//   const router = useRouter();
//   return (
//     <>
//       {router.isReady && router.query.ticket_id ? (
//         <SupportTicketDetails tickticketPropet_id={router.query.ticket_id as string} />
//       ) : (
//         <TicketEmptyState slotLottie={<>Loading</>} />
//       )}
//     </>
//   );
// }

// export default SupportTicket;
