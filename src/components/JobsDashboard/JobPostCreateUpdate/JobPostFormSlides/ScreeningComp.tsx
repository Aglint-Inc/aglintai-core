import React from 'react';

import { PhoneScreening } from '@/devlink2';

const ScreeningComp = () => {
  return (
    <>
      <PhoneScreening
        slotWelcomeText={<></>}
        slotQuestions={<></>}
        slotEndText={<></>}
      />
    </>
  );
};

export default ScreeningComp;
