import * as React from "react";
import * as Types from "./types";

declare function SessionAndTime(props: {
  as?: React.ElementType;
  textTime?: React.ReactNode;
  textSessionName?: React.ReactNode;
}): React.JSX.Element;
