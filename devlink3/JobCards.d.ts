import * as React from "react";
import * as Types from "./types";

declare function JobCards(props: {
  as?: React.ElementType;
  textRole?: React.ReactNode;
  textLocation?: React.ReactNode;
}): React.JSX.Element;
