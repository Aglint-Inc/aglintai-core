import * as React from "react";
import * as Types from "./types";

declare function InterviewLoadCard(props: {
  as?: React.ElementType;
  textHeading?: React.ReactNode;
  textInterviewCounts?: React.ReactNode;
  textLabel?: React.ReactNode;
}): React.JSX.Element;
