import * as React from "react";
import * as Types from "./types";

declare function Placehold(props: {
  as?: React.ElementType;
  textBlockText?: React.ReactNode;
  slotDocumenter?: React.ReactNode;
}): React.JSX.Element;
