import * as React from "react";
import * as Types from "./types";

declare function LoadedSlots(props: {
  as?: React.ElementType;
  slotLoadedSlotPill?: Types.Devlink.Slot;
  textDay?: React.ReactNode;
}): React.JSX.Element;
