import * as React from "react";
import * as Types from "./types";

declare function ResumeFeedbackScore(props: {
  as?: React.ElementType;
  textFeedback?: React.ReactNode;
  textScoreState?: React.ReactNode;
}): React.JSX.Element;
