import * as React from "react";
import * as Types from "./types";

declare function EmailChangePop(props: {
  as?: React.ElementType;
  onClickClose?: Types.Devlink.RuntimeProps;
  textDesc?: React.ReactNode;
}): React.JSX.Element;
