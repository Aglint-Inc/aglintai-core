import { DatabaseTable } from '@aglint/shared-types';

export const onUpdateCandidateRequestAvailability = ({
  new_data,
  old_data,
}: {
  new_data: DatabaseTable['request'];
  old_data: DatabaseTable['request'];
}) => {
  //
};
