import React from 'react';

import { GlobalIcon } from '@/devlink/GlobalIcon';

function Interviewers() {
  return (
    <GlobalIcon iconName={'supervisor_account'} size={6} weight={'regular'} />
  );
}

export default Interviewers;
