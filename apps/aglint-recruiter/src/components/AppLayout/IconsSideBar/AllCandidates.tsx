import React from 'react';

import { GlobalIcon } from '@/devlink/GlobalIcon';

function AllCandidates() {
  return <GlobalIcon iconName={'group'} size={6} weight={'regular'} />;
}

export default AllCandidates;
