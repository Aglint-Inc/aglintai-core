import * as React from "react";
import * as Types from "./types";

declare function NoPendingReq(props: {
  as?: React.ElementType;
  textHeader?: React.ReactNode;
  textDesc?: React.ReactNode;
}): React.JSX.Element;
