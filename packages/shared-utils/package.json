{
  "name": "@aglint/shared-utils",
  "scripts": {
    "build": "rimraf dist && tsup",
    "lint": "rimraf dist && tsup && tsc",
    "type-check": "tsc --noEmit",
    "dev": "rimraf dist && tsup --watch"
  },
  "version": "0.0.1",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "dependencies": {
    "@types/node": "^20.11.24",
    "dayjs": "^1.11.11",
    "node-html-parser": "^6.1.13",
    "tsup": "^8.0.2",
    "typescript": "latest",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@aglint/typescript-config": "*",
    "rimraf": "^6.0.1"
  }
}
